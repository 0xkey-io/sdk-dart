name: zeroxkey_encoding
description: |
  This package provides encoding and decoding functions used by other ZeroXKey packages. 
  Feel free to import the exported functions into your own apps, 
  but be aware that significant interface changes may occur in the future.
version: 0.1.1
repository: https://github.com/0xkey-io/sdk-dart

environment:
  sdk: ^3.6.1

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
