# Changelog

## 0.1.1
- Move shared stamper types (`TStamper`, `TStamp`) here to break the
  `zeroxkey_http` ↔ `zeroxkey_api_key_stamper` dependency cycle for pub.dev.

## 0.1.0 - Initial Release
- Base encoding and decoding utilities for the ZeroXKey SDK.
