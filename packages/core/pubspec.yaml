name: zeroxkey_core
description: Platform-agnostic core for the ZeroXKey SDK (domain, data, network, providers, DI).
version: 0.1.1
repository: https://github.com/0xkey-io/sdk-dart

environment:
  sdk: ^3.6.1

resolution: workspace

dependencies:
  http: ^1.3.0
  zeroxkey_http: ^0.1.0
  zeroxkey_crypto: ^0.1.0
  zeroxkey_encoding: ^0.1.0
  zeroxkey_api_key_stamper: ^0.1.0

dev_dependencies:
  test: ^1.25.0
